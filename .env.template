# --- Docker Compose Environment ---
COMPOSE_PROJECT_NAME=mywebapp_dev

# --- Backend Configuration ---
BACKEND_INTERNAL_PORT=8080
RUST_LOG=debug
DATABASE_URL=sqlite:/app/data/whoknows.db # Maybe use a different DB name for dev

# --- Frontend Configuration ---
FRONTEND_INTERNAL_PORT=80
HOST_PORT_FRONTEND=8080 # Use a different host port for local dev if 80 is taken

# --- Deployment Variables ---
# Not usually needed for local dev unless testing deploy scripts